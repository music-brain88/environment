name: build

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04, macos-latest]
    steps:
      - uses: actions/checkout@v1
      - name: build on ${{matrix.os}}
        run: |
          make compose-build 
